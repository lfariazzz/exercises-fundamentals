#include <stdio.h>
#include <stdbool.h>

int main(){
    #define N 3
    int i, j, vetor1[N], vetor2[N], vetor3[N * 2];
    for(int i = 0; i < N; i++){
        printf("Digite um valor para adicionar na %i a posicao vetor: ", i + 1);
        scanf("%i", &x);
    }

    for(int j = 0; j < N; j++){
        printf("Digite um valor para adicionar na %i a posicao vetor: ", j + 1);
        scanf("%i", &x);

    }
}
